name: nightly

on:
  schedule:
    # Daily at 18:00 UTC. Scheduled runs only fire on the default branch.
    - cron: '0 18 * * *'
  workflow_dispatch:
    inputs:
      revs:
        description: 'Specific changeset short-hashes to build (space-separated). Empty = auto-detect unbuilt revs.'
        required: false
        default: ''
      max:
        description: 'Max number of unbuilt revs to build in one run (backlog cap).'
        required: false
        default: '10'

permissions:
  contents: write

env:
  HG_URL: https://www.retrodev.com/repos/blastem
  # Recent clang (19+) and Apple clang promote several legacy C warnings to errors
  # by default. Upstream compiles cleanly on older clang, so downgrade just those
  # back to warnings (no source changes). Harmless for gcc/mingw.
  CWARN: '-Wno-error=incompatible-pointer-types -Wno-error=incompatible-function-pointer-types -Wno-error=int-conversion -Wno-error=implicit-int'
  SDL_WIN_VER: '2.30.9'
  GLEW_WIN_VER: '2.2.0'

jobs:
  # ---------------------------------------------------------------------------
  # Clone upstream ONCE, detect every changeset that does not yet have a
  # `nightly-<node>` release (oldest-first, capped), and publish a per-rev
  # source tarball + release-notes markdown as an artifact for the build jobs.
  # ---------------------------------------------------------------------------
  coordinate:
    runs-on: ubuntu-latest
    outputs:
      revs: ${{ steps.detect.outputs.revs }}
      has_work: ${{ steps.detect.outputs.has_work }}
    steps:
      - name: Install mercurial
        run: sudo apt-get update && sudo apt-get install -y mercurial

      - name: Clone upstream
        run: |
          set -euo pipefail
          for i in 1 2 3 4 5; do
            hg clone -U "$HG_URL" upstream && break
            echo "clone attempt $i failed; retrying in 20s..." >&2
            sleep 20
          done
          [ -d upstream/.hg ] || { echo "::error::Failed to clone upstream"; exit 1; }

      - name: Detect unbuilt revisions
        id: detect
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          MANUAL: ${{ github.event.inputs.revs }}
          MAXIN: ${{ github.event.inputs.max }}
        run: |
          set -euo pipefail
          gh release list --repo "$REPO" --limit 1000 --json tagName -q '.[].tagName' \
            > /tmp/tags.txt 2>/dev/null || true
          hg -R upstream log -r 'reverse(0:tip)' --template '{node|short}\n' > /tmp/nodes.txt

          cat > /tmp/detect.py <<'PY'
          import json, os, sys
          built = set()
          try:
              for line in open('/tmp/tags.txt'):
                  line = line.strip()
                  if line.startswith('nightly-'):
                      built.add(line[len('nightly-'):])
          except FileNotFoundError:
              pass
          nodes = [l.strip() for l in open('/tmp/nodes.txt') if l.strip()]  # newest first
          manual = os.environ.get('MANUAL', '').split()
          try:
              cap = int(os.environ.get('MAXIN') or '10')
          except ValueError:
              cap = 10
          if manual:
              todo = manual
          else:
              todo = []
              for n in nodes:
                  if n in built:
                      break
                  todo.append(n)
                  if len(todo) >= cap:
                      print(f"::warning::Reached cap of {cap} unbuilt revs; "
                            f"older unbuilt revs will be built on later runs.", file=sys.stderr)
                      break
              todo.reverse()  # chronological (oldest first)
          revs = [{"node": n} for n in todo]
          with open(os.environ['GITHUB_OUTPUT'], 'a') as out:
              out.write("revs=" + json.dumps(revs) + "\n")
              out.write("has_work=" + ("true" if revs else "false") + "\n")
          with open('/tmp/todo.txt', 'w') as f:
              f.write("".join(n + "\n" for n in todo))
          print("Revisions to build:", json.dumps(revs), file=sys.stderr)
          PY
          python3 /tmp/detect.py

      - name: Archive sources and release notes
        if: steps.detect.outputs.has_work == 'true'
        run: |
          set -euo pipefail
          mkdir -p sources
          while read -r n || [ -n "$n" ]; do
            [ -n "$n" ] || continue
            hg archive -R upstream -r "$n" -t tgz -p src "sources/src-$n.tgz"
            author=$(hg -R upstream log -r "$n" --template '{author}')
            cdate=$(hg -R upstream log -r "$n" --template '{date|isodate}')
            hg -R upstream log -r "$n" --template '{desc}' > /tmp/desc.txt
            {
              echo "Unofficial automated build of BlastEm at upstream changeset [\`$n\`](https://www.retrodev.com/repos/blastem/rev/$n)."
              echo
              echo "- **Author:** $author"
              echo "- **Date:** $cdate"
              echo "- **Changeset:** [\`$n\`](https://www.retrodev.com/repos/blastem/rev/$n)"
              echo
              echo "**Commit message**"
              echo
              echo '```'
              cat /tmp/desc.txt
              echo
              echo '```'
              echo
              echo "---"
              echo "Source is unmodified upstream. BlastEm is GPLv3, created by Michael Pavone."
            } > "sources/body-$n.md"
            echo "prepared $n"
          done < /tmp/todo.txt
          ls -la sources

      - name: Upload sources artifact
        if: steps.detect.outputs.has_work == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: sources
          path: |
            sources/*.tgz
            sources/*.md
          retention-days: 2

  # ---------------------------------------------------------------------------
  # WebAssembly (WebGL).
  # ---------------------------------------------------------------------------
  wasm:
    needs: coordinate
    if: needs.coordinate.outputs.has_work == 'true'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        rev: ${{ fromJSON(needs.coordinate.outputs.revs) }}
    steps:
      - name: Install tools
        run: sudo apt-get update && sudo apt-get install -y zip fonts-dejavu-core
      - name: Setup emscripten
        uses: mymindstorm/setup-emsdk@v14
      - name: Download sources
        uses: actions/download-artifact@v4
        with:
          name: sources
      - name: Extract source @ ${{ matrix.rev.node }}
        run: |
          set -euo pipefail
          tar xzf "src-${{ matrix.rev.node }}.tgz"
          sed -i -E "s/(define BLASTEM_VERSION \"[^-]+)-pre\"/\1-pre-${{ matrix.rev.node }}\"/" src/version.inc || true
      - name: Build (wasm)
        working-directory: src
        run: |
          set -euo pipefail
          # Upstream embeds DroidSans.ttf at link time but the font is not in the repo.
          # font_web.c loads it by literal name, so any TrueType font works.
          cp /usr/share/fonts/truetype/dejavu/DejaVuSans.ttf DroidSans.ttf
          make CC=emcc CPU=wasm m68k.c z80.c
          make CC="emcc $CWARN" CPU=wasm blastem.html
      - name: Package
        working-directory: src
        run: |
          set -euo pipefail
          ver=$(sed -E -n 's/^[^B]+BLASTEM_VERSION "([^"]+)"/\1/p' version.inc)
          dir="blastem-wasm-${ver}"
          mkdir "$dir"
          for f in blastem.html blastem.js blastem.wasm blastem.data; do
            [ -f "$f" ] && cp "$f" "$dir"/ || true
          done
          cp blastem.html "$dir"/index.html
          cp COPYING "$dir"/COPYING
          zip -r "${dir}.zip" "$dir"
      - name: Publish
        uses: softprops/action-gh-release@v2
        with:
          tag_name: nightly-${{ matrix.rev.node }}
          name: BlastEm nightly ${{ matrix.rev.node }}
          prerelease: true
          body_path: body-${{ matrix.rev.node }}.md
          files: src/blastem-wasm-*.zip
          fail_on_unmatched_files: true

  # ---------------------------------------------------------------------------
  # macOS: build each arch natively (brew + dylibbundler), then lipo into one
  # Universal .app.
  # ---------------------------------------------------------------------------
  macos-build:
    needs: coordinate
    if: needs.coordinate.outputs.has_work == 'true'
    runs-on: ${{ matrix.arch == 'arm64' && 'macos-15' || 'macos-13' }}
    strategy:
      fail-fast: false
      matrix:
        rev: ${{ fromJSON(needs.coordinate.outputs.revs) }}
        arch: [x86_64, arm64]
    steps:
      - name: Install dependencies
        run: brew install sdl2 glew pkg-config dylibbundler
      - name: Download sources
        uses: actions/download-artifact@v4
        with:
          name: sources
      - name: Extract source @ ${{ matrix.rev.node }}
        run: |
          set -euo pipefail
          tar xzf "src-${{ matrix.rev.node }}.tgz"
      - name: Build (${{ matrix.arch }})
        working-directory: src
        run: |
          set -euo pipefail
          make CC="cc $CWARN" blastem
          file blastem
      - name: Stage binary + bundled dylibs
        working-directory: src
        run: |
          set -euo pipefail
          rm -rf stage && mkdir -p stage/Frameworks
          cp blastem stage/
          dylibbundler -od -b -x stage/blastem \
            -d stage/Frameworks -p "@executable_path/../Frameworks/"
          tar -cf "macstage-${{ matrix.arch }}.tar" -C stage .
      - name: Upload arch stage
        uses: actions/upload-artifact@v4
        with:
          name: macstage-${{ matrix.arch }}-${{ matrix.rev.node }}
          path: src/macstage-${{ matrix.arch }}.tar
          retention-days: 2

  macos-universal:
    needs: [coordinate, macos-build]
    if: needs.coordinate.outputs.has_work == 'true'
    runs-on: macos-15
    strategy:
      fail-fast: false
      matrix:
        rev: ${{ fromJSON(needs.coordinate.outputs.revs) }}
    steps:
      - name: Download sources
        uses: actions/download-artifact@v4
        with:
          name: sources
      - name: Download arch stages
        uses: actions/download-artifact@v4
        with:
          pattern: macstage-*-${{ matrix.rev.node }}
          path: stages
      - name: Build Universal .app
        run: |
          set -euo pipefail
          tar xzf "src-${{ matrix.rev.node }}.tgz"
          sed -i '' -E "s/(define BLASTEM_VERSION \"[^-]+)-pre\"/\1-pre-${{ matrix.rev.node }}\"/" src/version.inc || true
          ver=$(sed -E -n 's/^[^B]+BLASTEM_VERSION "([^"]+)"/\1/p' src/version.inc)

          mkdir -p arm x86
          tar xf "stages/macstage-arm64-${{ matrix.rev.node }}/macstage-arm64.tar" -C arm
          tar xf "stages/macstage-x86_64-${{ matrix.rev.node }}/macstage-x86_64.tar" -C x86

          APP="BlastEm.app"
          rm -rf "$APP"
          mkdir -p "$APP/Contents/MacOS" "$APP/Contents/Frameworks"
          lipo -create arm/blastem x86/blastem -output "$APP/Contents/MacOS/blastem"
          for f in arm/Frameworks/*; do
            name=$(basename "$f")
            if [ -f "x86/Frameworks/$name" ]; then
              lipo -create "$f" "x86/Frameworks/$name" -output "$APP/Contents/Frameworks/$name" 2>/dev/null \
                || cp "$f" "$APP/Contents/Frameworks/$name"
            else
              cp "$f" "$APP/Contents/Frameworks/$name"
            fi
          done
          # Data assets (read from the executable's directory by blastem).
          cp -R src/shaders src/images src/default.cfg src/rom.db src/gamecontrollerdb.txt src/systems.cfg src/COPYING "$APP/Contents/MacOS/"
          cat > "$APP/Contents/Info.plist" <<PLIST
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0"><dict>
            <key>CFBundleName</key><string>BlastEm</string>
            <key>CFBundleDisplayName</key><string>BlastEm</string>
            <key>CFBundleIdentifier</key><string>com.retrodev.blastem.unofficial</string>
            <key>CFBundleVersion</key><string>${ver}</string>
            <key>CFBundleShortVersionString</key><string>${ver}</string>
            <key>CFBundleExecutable</key><string>blastem</string>
            <key>CFBundlePackageType</key><string>APPL</string>
            <key>LSMinimumSystemVersion</key><string>10.13</string>
            <key>NSHighResolutionCapable</key><true/>
          </dict></plist>
          PLIST
          codesign --force --deep -s - "$APP" || true
          echo "=== verify universal ==="; lipo -info "$APP/Contents/MacOS/blastem"
          ditto -c -k --sequesterRsrc --keepParent "$APP" "BlastEm-macOS-${ver}.zip"
          ls -la "BlastEm-macOS-${ver}.zip"
      - name: Publish
        uses: softprops/action-gh-release@v2
        with:
          tag_name: nightly-${{ matrix.rev.node }}
          name: BlastEm nightly ${{ matrix.rev.node }}
          prerelease: true
          body_path: body-${{ matrix.rev.node }}.md
          files: BlastEm-macOS-*.zip
          fail_on_unmatched_files: true

  # ---------------------------------------------------------------------------
  # Linux x86_64 (JIT) and arm64 (interpreter), native runners.
  # Portable: bundle libSDL2/libGLEW next to the binary with $ORIGIN/lib rpath.
  # ---------------------------------------------------------------------------
  linux:
    needs: coordinate
    if: needs.coordinate.outputs.has_work == 'true'
    runs-on: ${{ matrix.arch == 'arm64' && 'ubuntu-24.04-arm' || 'ubuntu-latest' }}
    strategy:
      fail-fast: false
      matrix:
        rev: ${{ fromJSON(needs.coordinate.outputs.revs) }}
        arch: [x86_64, arm64]
    steps:
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential python3 libsdl2-dev libglew-dev zlib1g-dev patchelf zip file
      - name: Download sources
        uses: actions/download-artifact@v4
        with:
          name: sources
      - name: Extract source @ ${{ matrix.rev.node }}
        run: |
          set -euo pipefail
          tar xzf "src-${{ matrix.rev.node }}.tgz"
          sed -i -E "s/(define BLASTEM_VERSION \"[^-]+)-pre\"/\1-pre-${{ matrix.rev.node }}\"/" src/version.inc || true
      - name: Build (${{ matrix.arch }})
        working-directory: src
        run: |
          set -euo pipefail
          make CC="gcc $CWARN" blastem
          file blastem
      - name: Package (portable)
        working-directory: src
        run: |
          set -euo pipefail
          ver=$(sed -E -n 's/^[^B]+BLASTEM_VERSION "([^"]+)"/\1/p' version.inc)
          dir="blastem-linux-${{ matrix.arch }}-${ver}"
          mkdir -p "$dir/lib"
          cp blastem "$dir/"
          cp -r shaders images default.cfg rom.db gamecontrollerdb.txt systems.cfg COPYING "$dir/"
          # Bundle the non-system libraries (SDL2, GLEW); leave GL/X/audio to the host.
          ldd blastem | awk '/libSDL2|libGLEW/ {print $3}' | while read -r p; do
            [ -n "$p" ] && cp -Lv "$p" "$dir/lib/"
          done
          patchelf --set-rpath '$ORIGIN/lib' "$dir/blastem"
          tar czf "${dir}.tar.gz" "$dir"
          ls -la "${dir}.tar.gz"
      - name: Publish
        uses: softprops/action-gh-release@v2
        with:
          tag_name: nightly-${{ matrix.rev.node }}
          name: BlastEm nightly ${{ matrix.rev.node }}
          prerelease: true
          body_path: body-${{ matrix.rev.node }}.md
          files: src/blastem-linux-*.tar.gz
          fail_on_unmatched_files: true

  # ---------------------------------------------------------------------------
  # Linux x86 (32-bit, JIT) via multilib.
  # ---------------------------------------------------------------------------
  linux-i686:
    needs: coordinate
    if: needs.coordinate.outputs.has_work == 'true'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        rev: ${{ fromJSON(needs.coordinate.outputs.revs) }}
    steps:
      - name: Install dependencies
        run: |
          sudo dpkg --add-architecture i386
          sudo apt-get update
          sudo apt-get install -y build-essential gcc-multilib python3 patchelf zip file \
            libsdl2-dev:i386 libglew-dev:i386 zlib1g-dev:i386
      - name: Download sources
        uses: actions/download-artifact@v4
        with:
          name: sources
      - name: Extract source @ ${{ matrix.rev.node }}
        run: |
          set -euo pipefail
          tar xzf "src-${{ matrix.rev.node }}.tgz"
          sed -i -E "s/(define BLASTEM_VERSION \"[^-]+)-pre\"/\1-pre-${{ matrix.rev.node }}\"/" src/version.inc || true
      - name: Build (i686)
        working-directory: src
        env:
          PKG_CONFIG_PATH: /usr/lib/i386-linux-gnu/pkgconfig
          PKG_CONFIG_LIBDIR: /usr/lib/i386-linux-gnu/pkgconfig:/usr/share/pkgconfig
        run: |
          set -euo pipefail
          make CPU=i686 CC="gcc $CWARN" blastem
          file blastem
      - name: Package (portable)
        working-directory: src
        run: |
          set -euo pipefail
          ver=$(sed -E -n 's/^[^B]+BLASTEM_VERSION "([^"]+)"/\1/p' version.inc)
          dir="blastem-linux-i686-${ver}"
          mkdir -p "$dir/lib"
          cp blastem "$dir/"
          cp -r shaders images default.cfg rom.db gamecontrollerdb.txt systems.cfg COPYING "$dir/"
          ldd blastem | awk '/libSDL2|libGLEW/ {print $3}' | while read -r p; do
            [ -n "$p" ] && cp -Lv "$p" "$dir/lib/"
          done
          patchelf --set-rpath '$ORIGIN/lib' "$dir/blastem"
          tar czf "${dir}.tar.gz" "$dir"
          ls -la "${dir}.tar.gz"
      - name: Publish
        uses: softprops/action-gh-release@v2
        with:
          tag_name: nightly-${{ matrix.rev.node }}
          name: BlastEm nightly ${{ matrix.rev.node }}
          prerelease: true
          body_path: body-${{ matrix.rev.node }}.md
          files: src/blastem-linux-i686-*.tar.gz
          fail_on_unmatched_files: true

  # ---------------------------------------------------------------------------
  # Windows x86_64 and x86 (32-bit), cross-compiled with mingw-w64 (as upstream).
  # ---------------------------------------------------------------------------
  windows:
    needs: coordinate
    if: needs.coordinate.outputs.has_work == 'true'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        rev: ${{ fromJSON(needs.coordinate.outputs.revs) }}
        arch: [x86_64, i686]
    steps:
      - name: Install mingw + tools
        run: |
          sudo apt-get update
          sudo apt-get install -y mingw-w64 python3 zip unzip wget
      - name: Download sources
        uses: actions/download-artifact@v4
        with:
          name: sources
      - name: Extract source @ ${{ matrix.rev.node }}
        run: |
          set -euo pipefail
          tar xzf "src-${{ matrix.rev.node }}.tgz"
          sed -i -E "s/(define BLASTEM_VERSION \"[^-]+)-pre\"/\1-pre-${{ matrix.rev.node }}\"/" src/version.inc || true
      - name: Fetch Windows SDL2 + GLEW dev libraries
        working-directory: src
        run: |
          set -euo pipefail
          wget -q "https://github.com/libsdl-org/SDL/releases/download/release-${SDL_WIN_VER}/SDL2-devel-${SDL_WIN_VER}-mingw.tar.gz"
          mkdir sdl && tar xzf "SDL2-devel-${SDL_WIN_VER}-mingw.tar.gz" -C sdl --strip-components=1
          wget -q "https://github.com/nigels-com/glew/releases/download/glew-${GLEW_WIN_VER}/glew-${GLEW_WIN_VER}-win32.zip"
          unzip -q "glew-${GLEW_WIN_VER}-win32.zip"
          mv "glew-${GLEW_WIN_VER}" glew
      - name: Build (${{ matrix.arch }})
        working-directory: src
        run: |
          set -euo pipefail
          make PORTABLE=1 OS=Windows CPU=${{ matrix.arch }} SDL=sdl2 blastem.exe
          file blastem.exe
      - name: Package
        working-directory: src
        run: |
          set -euo pipefail
          ver=$(sed -E -n 's/^[^B]+BLASTEM_VERSION "([^"]+)"/\1/p' version.inc)
          if [ "${{ matrix.arch }}" = "x86_64" ]; then triple=x86_64-w64-mingw32; suffix=win64; else triple=i686-w64-mingw32; suffix=win32; fi
          dir="blastem-${suffix}-${ver}"
          mkdir "$dir"
          cp blastem.exe "$dir/"
          cp "sdl/${triple}/bin/SDL2.dll" "$dir/"
          cp -r shaders images default.cfg rom.db gamecontrollerdb.txt systems.cfg "$dir/"
          cp COPYING "$dir/COPYING.txt"
          zip -r "${dir}.zip" "$dir"
          ls -la "${dir}.zip"
      - name: Publish
        uses: softprops/action-gh-release@v2
        with:
          tag_name: nightly-${{ matrix.rev.node }}
          name: BlastEm nightly ${{ matrix.rev.node }}
          prerelease: true
          body_path: body-${{ matrix.rev.node }}.md
          files: src/blastem-win*.zip
          fail_on_unmatched_files: true
